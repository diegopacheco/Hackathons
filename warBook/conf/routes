# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.index

# Achievements
GET	    /achievements			    controllers.AchievementController.list
POST    /achievements               controllers.AchievementController.newAchievement

# Guerreiros
GET     /guerreiros                 controllers.Guerreiro.guerreiro
POST    /guerreiros                 controllers.Guerreiro.newGuerreiro
POST    /guerreiros/:id/delete      controllers.Guerreiro.deleteGuerreiro(id: Long)

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
